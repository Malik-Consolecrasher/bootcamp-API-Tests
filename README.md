Site that retrieves information on a random meal from API (TheMealDB), and displays it on screan

Uses DOM to allow meals to display on the site, and always an input to be made to remove them from the site.
